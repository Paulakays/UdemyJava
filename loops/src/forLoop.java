public class forLoop {
    public static void main (String []args){
        for(int x = 0; x < 10; x++){
            System.out.println(x);
            /*
            Every time x is executed a post increment is run(x++) x will now be 1.
            The code is checked again to ensure that the count number is not less than 10
            It keeps on being executed till x is less than 10 but the highest value before 10
             */
        }
    }
}
